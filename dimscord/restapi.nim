## This is where you interact with the api:
## `sendMessages`, `deleteMessages`, `getGuild`, `banGuildMember` etc.
import restapi/[requester, guild, channel, message, user]
export requester, guild, channel, message, user